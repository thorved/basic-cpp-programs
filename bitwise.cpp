#include <iostream>
using namespace std;
int main()
{
    // int z, a = 10, b = 12;
    // z = a & b;
    // // z=a|b;
    // // z=a^b;
    // cout << z;
    int a = 10;
    int b;
    // b = a >> 1; //the number will divided by 2.
    b = a << 1; //the no. will multiply by 2.
    // cout << b;
    b = ~a;
       cout << b;
     // the bit number will bw changed 1 will be instead of 0 and 0 will bw instead of 1.and find that's decimal number and will print them.

    return 0;
}